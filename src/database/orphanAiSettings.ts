/** Offline GGUF leftovers — deleted once on migrate. */
export const ORPHAN_AI_SETTING_KEYS = [
  "ai_mode",
  "ai_model_version",
  "ai_model_sha256",
  "ai_model_installed_at",
  "ai_model_size",
  "ai_active_model_id",
  "ai_keep_in_memory",
] as const;
